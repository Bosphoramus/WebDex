import styles from '~/styles/presentationAnimation.module.css'

export default function ProjectPresentationAnimation() {
    return (
        <div>
            <div className={`${styles.container}`}>
                <h1 className={`${styles.brandText}`}>WebDex</h1>
            </div>
        </div>
    )
}
